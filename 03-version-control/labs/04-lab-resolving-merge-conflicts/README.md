# 04 - Resolving Merge Conflicts in GitHub Using Branches

## Objective
Practice resolving merge conflicts using branches in GitHub and Git.

## Files
- `file.txt` – file used to simulate a merge conflict.

## Steps Completed
1. Created a new branch `lab-merge-conflict`.
2. Added `file.txt` with initial content and committed.
3. Created a `feature-branch` and edited `file.txt`.
4. Edited `file.txt` also in `lab-merge-conflict` branch to create a conflict.
5. Merged `feature-branch` into `lab-merge-conflict` and resolved the conflict.
6. Merged `lab-merge-conflict` into `main`.
7. Deleted `lab-merge-conflict` branch locally and remotely.

## Status
Exercise completed successfully.
