# Creating and Importing Components

## Description
This lab demonstrates how to move a React component into its own file and import it into a parent component using ES module exports and imports.

## Tasks Completed
- Moved the `Heading` component into a separate file
- Exported the `Heading` component
- Imported `Heading` into the `App` component
- Rendered the imported component successfully

## Status
Lab completed successfully.  
**Grade:** 100%
